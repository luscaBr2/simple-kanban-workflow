{
    "version": 2,
    "builds": [
        {
            "src": "api/src/server.ts",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["api/tasks.json"]
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "api/src/server.ts"
        },
        {
            "src": "/(.*)",
            "dest": "index.html"
        }
    ]
}
